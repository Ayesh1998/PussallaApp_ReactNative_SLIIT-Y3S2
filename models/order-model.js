class OrderModel {
  constructor(orderNo, amount, status, year, month, date) {
    this.orderNo = orderNo
    this.amount = amount
    this.status = status
    this.year = year
    this.month = month
    this.date = date
  }
}

export default OrderModel
